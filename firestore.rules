
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Regras de desenvolvimento: permite acesso total. 
      // Em produção, deve-se restringir por auth.uid
      allow read, write: if true;
    }
  }
}
